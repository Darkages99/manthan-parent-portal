import { ModulePlaceholder } from "@/components/module-placeholder";

export default function StaffDefaulters() {
  return (
    <ModulePlaceholder
      title="Defaulters"
      phase="Coming soon"
      description="Logging discipline incidents against a student, visible to that student's guardians and concerned staff. This will get its own dedicated workflow; we'll get to it."
    />
  );
}
