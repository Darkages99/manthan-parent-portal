import { ModulePlaceholder } from "@/components/module-placeholder";

export default function PaymentsPage() {
  return (
    <ModulePlaceholder
      title="Fees & payments"
      phase="Phase 2"
      description="Fee schedule, dues, part-payments and receipts per child — waiting on a confirmed gateway (Razorpay/PayU) and merchant account."
    />
  );
}
