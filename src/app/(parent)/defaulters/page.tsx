import { ModulePlaceholder } from "@/components/module-placeholder";

export default function DefaultersPage() {
  return (
    <ModulePlaceholder
      title="Defaulters"
      phase="Coming soon"
      description="A digitised discipline log for your child — incident, date and action taken. This will get its own dedicated workflow; we'll get to it."
    />
  );
}
